package com.example.theuncleandgioppeproject.repository
import kotlinx.coroutines.flow.flow
import javax.inject.Inject

/*

class LoginRepository @Inject constructor(private val loginService: LoginService) : BaseRepository() {

 */
/*   @Inject
    fun login(username: String, password: String): Flow<NetworkResponse<LoginResponse, BaseApiErrorResponse>> = flow {
        emit(loginService.login(LoginBody(username,password)))
    }
*/


